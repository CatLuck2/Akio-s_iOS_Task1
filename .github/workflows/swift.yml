name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Slack success notification
      run: | 
        curl -X POST --data-urlencode 'payload={"text": ":tada::tada::tada:${{github.repository}}: Build and Test succeeded！:tada::tada::tada:"}' ${{secrets.SLACK_WEBHOOK_URL}}
#       - name: Slack Notification
#         uses: CatLuck2/Akio-s_iOS_Task1@master
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#         with:
#            payload: |  
#               { "text": "Star: ${{ github.event.repository.stargazers_count }} ${{ github.event.repository.html_url }}" }
